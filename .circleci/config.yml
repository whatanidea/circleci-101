version: 2
jobs:
    docker-image:
      steps:
        - checkout
        - run:
          name: docker-image
          command: |
          docker build -f .
          docker login https://cloud.docker.com/repository/docker/fhdockerlab/ -u fhdockerlab -p Fasank12!23
          docker tag simple-html:v1 fhdockerlab/simple-html:v2
          docker push fhdockerlab/simple-html:v2

workflows:
  version: 2
  build:
    jobs:
      - docker-image
                     