version: 2
jobs:
    build:
      docker:
        - image: circleci:node:4.8.2
          steps:
        - checkout  
        - run : echo "hell0 world"
        

workflows:
  version: 2
  build:
    jobs:
      - build
                     